.Content
  = item.name
  = item.price
  = product.images.each do |image|
    = image_tag image.src.url

-# .wrapper
-#   = render "top_header"
-#   = render "top_block"
-#   = render "pickup_category"
-#   = render "bottom_block"
-#   = render "bottom_footer"
-#   = render "display_btn"